name: life_quest_rpg
description: "Gamified RPG for KitaHack 2026"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Matches the specific requirements from your error log
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4 # <--- FIXED: Updated from 5.0.0 to 6.1.4
  firebase_ai: ^3.7.0
  cloud_firestore: ^6.1.2
  google_fonts: ^6.2.1
  flutter_dotenv: ^5.2.1
  confetti: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
