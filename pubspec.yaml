name: life_quest_rpg
description: "Gamified RPG for KitaHack 2026"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # The 2026 Compatible Stack
  firebase_core: ^4.4.0
  firebase_ai: ^3.7.0 # Unified AI SDK
  cloud_firestore: ^6.1.2 # Matches Core 4.4.0
  google_fonts: ^6.2.1
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
